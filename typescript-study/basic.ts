const user = {
	firstName: "Nuri",
};

function divide(a: number, b: number) {
	return a / b;
}

const nuri1 = { name: "nuri" };
// nuri.hello(); => TS takes it as an error!
/*
    The main benefit of TypeScript is that it can highlight unexpected behavior in your code, lowering the chance of bugs.
    
    runtime error  - errors happened in console, only in user's computer
*/
